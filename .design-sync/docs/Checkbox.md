---
category: Forms
---

# Checkbox

Native checkbox at `--icon-md`, tinted through CSS `accent-color`. `accentColor` defaults to `var(--ink)`; pass a category token (`var(--cat-business-solid)`) when the control belongs to a categorised object.

The dominant composition is a task row: checkbox, then title, with completed rows struck through in `--ink-faint`.
