---
category: Keyboard
---

# KeyCap

A keyboard hint rendered as a visible cap: `--surface-2`, hairline border, caption size (D38 / V5).

Chords are **one cap, not three** — pass the whole string (`⌘⇧K`). Caps sit on the baseline inside running copy.
