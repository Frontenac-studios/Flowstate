---
category: Keyboard
---

# ShortcutHint

Label first, then the `KeyCap` — the app-wide hint order. Use this instead of hand-pairing a label with a `KeyCap` so the order never drifts.

```jsx
<ShortcutHint label="Capture" keys="⌘⇧K" />
```
