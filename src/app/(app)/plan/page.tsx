import QuarterSurface from "@/components/kash/quarter/QuarterSurface";

/**
 * The Quarter surface (MISSION law 4c), rebuilt from the placeholder stub. Renders
 * at /plan — the reserved home — as a bare page column, matching Money; the pinned
 * coach dock arrives when chat un-parks (a "plan" coach surface is added then).
 */
export default function PlanningPage() {
  return <QuarterSurface />;
}
